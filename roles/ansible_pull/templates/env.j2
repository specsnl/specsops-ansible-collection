# Managed by Ansible — specsnl.specsops.ansible_pull
# Read by ansible-pull.service. An empty ANSIBLE_PULL_REPO makes that service a
# no-op, which is how the image ships.
ANSIBLE_PULL_REPO={{ ansible_pull_repo }}
ANSIBLE_PULL_BRANCH={{ ansible_pull_branch }}
