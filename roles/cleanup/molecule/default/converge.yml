---
- name: Converge
  hosts: all
  become: true
  tasks:
    - name: Include cleanup role
      ansible.builtin.include_role:
        name: specsnl.specsops.cleanup
      vars:
        cleanup_paths:
          - /tmp
          - /var/tmp
          - /var/lib/apt/lists
          - /srv/cleanup-volume
